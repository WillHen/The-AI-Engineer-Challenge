{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build && rm -rf ../api/static && mkdir -p ../api/static && cp -R out/* ../api/static/",
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  }
}
